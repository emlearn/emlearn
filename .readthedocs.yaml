version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"
  jobs:
    pre_build:
      - pip install ./ -v
    post_checkout:
      - git fetch --unshallow || true

python:
   install:
   - requirements: requirements.dev.txt

